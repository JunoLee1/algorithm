var canVisitAllRoom = function(rooms){
    const seen = new Set([0])// already visited 0 -th room
    const stack = [0];

    while (stack.length > 0){
        const prev_room = stack.pop();
    }
}
console.log(canVisitAllRoom([[1],[2],[3],[]]))